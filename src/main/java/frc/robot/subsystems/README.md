This directory should contain subdirectories for each subsystem

Each subdirectory should contain:
- The subsystem itself
- All simple commands that require only that subsystem
- The subsystem should also have syntactic sugar methods added to it for each command